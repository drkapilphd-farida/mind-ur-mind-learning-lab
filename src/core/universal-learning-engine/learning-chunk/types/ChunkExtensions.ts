// Learning Chunk™ (canonical domain model). The standard open
// extensibility escape hatch — real, always `{}` this sprint. Reserved
// for future needs this model didn't anticipate, without requiring a
// breaking schema change to add them.
export type ChunkExtensions = Readonly<Record<string, unknown>>
