// Universal Learning Intelligence Engine™ (ULIE™) — Learning Assets
// Generator™ (Reading Intelligence Engine™ Upgrade, Sprint-2). The one
// import path every downstream consumer uses — never import from
// types/internal directly.

export { buildLearningAssets } from './buildLearningAssets'
export type { BuildLearningAssetsOptions } from './buildLearningAssets'

export type { LearningAssetObject, KeywordAsset, WordAsset, PhraseAssetType, PhraseAsset, SentenceAsset, ParagraphAsset, LearningAssetBundle } from './types'
