// The Sprint 12 brief's own Retention policies, verbatim. See
// retention/computeExpiresAt.ts for the exact TTL each maps to.
export type MemoryRetention = 'session' | 'daily' | 'weekly' | 'monthly' | 'permanent'
