// Independently declared to mirror `provider-translation-engine`'s own
// `ProviderProfileId` exactly — same "structurally compatible,
// independently declared" convention used throughout this session.
export type ProviderResponseProfileId = 'openai' | 'anthropic' | 'gemini'
