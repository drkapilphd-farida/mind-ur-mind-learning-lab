// "Greeting Context, Learning Summary, Active Recommendation Summary,
// Next Action, Progress Summary, Motivation Metadata" — the Sprint 29
// brief's own Section 3 list, verbatim, and the fixed order every
// response's sections are always composed in.
export type MentorResponseSectionType =
  | 'greeting-context'
  | 'learning-summary'
  | 'active-recommendation-summary'
  | 'next-action'
  | 'progress-summary'
  | 'motivation-metadata'
