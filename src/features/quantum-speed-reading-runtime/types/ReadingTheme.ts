// Quantum Speed Reading™ Production Sprint-3. Real, new, presentation-
// only state — never a duplicate of any session/runtime concept. A
// learner's theme choice has no relationship to `AdaptiveRuntimeState`,
// `SessionSnapshot`, or anything persisted; it lives entirely client-side
// (`ReadingWorkspace`'s own local state).
export type ReadingTheme = 'light' | 'dark' | 'sepia'
