// "Reading Level™ / Memory Level™ / Focus Level™" — a coarse tier, not
// a numeric score (Mind Score is the only numeric input). Independently
// declared, same 4-tier vocabulary used elsewhere in this codebase by
// convention, not by import.
export type SkillLevel = 'beginner' | 'intermediate' | 'advanced' | 'expert'
