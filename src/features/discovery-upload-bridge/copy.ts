// Discovery → Upload Bridge™ (ALS-14 Sprint-1) — locked, static copy.
// Every line here is a fixed, single choice already selected from the
// brief's own example list (never a fabricated "AI decided" claim —
// there is no real per-user signal that should honestly vary these,
// unlike the Learning Potential Reveal's identity/outcome-message copy,
// which does read real cross-feature data).

// "AI chooses one" — this one, because it ties directly to the Golden
// Rule ("learning has already become personalized") rather than framing
// the document itself as the starting point.
export const SUPPORTING_LINE = 'Ready to personalize your learning.'

export const HERO_CARD_TAGS = ['Books', 'Notes', 'PDFs', 'Research Papers', 'Presentations', 'Study Material'] as const

// Ties directly to the real Learning Profile the user just saw, rather
// than a generic platitude.
export const MICRO_TRUST_MESSAGE = 'Your Learning Profile will guide every recommendation.'

export const WHAT_HAPPENS_NEXT_CARDS = [
  { emoji: '⚡', label: 'Smart Reading' },
  { emoji: '🧠', label: 'Memory Boost' },
  { emoji: '🎯', label: 'Focus Sessions' },
  { emoji: '💬', label: 'Ask Dr. Kapil' },
] as const

// One unified line, never three separate product badges — "Present them
// as one unified AI Learning System."
export const POWERED_BY_LINE = 'Quantum Speed Reading™ · Memory Intelligence™ · Focus Intelligence™'
