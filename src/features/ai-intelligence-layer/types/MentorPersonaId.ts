// The 6 mentor personas this sprint ships, mock only — no real
// personality/LLM-behavior difference exists yet, just distinct,
// deterministic persona *data* a future real provider call would be
// steered by.
export type MentorPersonaId = 'friendly-mentor' | 'reading-coach' | 'memory-coach' | 'focus-coach' | 'parent-guide' | 'teacher-mode'
