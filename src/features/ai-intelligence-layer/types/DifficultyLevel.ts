// Independently declared here rather than imported from any exercise-
// engine tier concept — this layer takes no dependency on the Learning
// Platform's own systems ("Future Provider Agnostic" cuts both ways:
// agnostic of what feeds it too).
export type DifficultyLevel = 'beginner' | 'intermediate' | 'advanced' | 'expert'
