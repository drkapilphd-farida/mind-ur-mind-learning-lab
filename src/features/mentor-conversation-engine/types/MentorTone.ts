// MentorTone™ — the *adjustable* aspect (varies per ConversationType),
// distinct from MentorPersonality™ (fixed, always-on traits). See
// personality/TONE_BY_CONVERSATION_TYPE.ts for the deterministic
// mapping from ConversationType to tone.
export type MentorTone = 'warm' | 'celebratory' | 'reassuring' | 'motivating' | 'neutral' | 'direct'
