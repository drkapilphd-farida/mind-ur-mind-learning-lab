// The 5 always-on traits from the Sprint 10 brief's "Personality"
// section. "Never manipulative" is deliberately NOT a trait here — it's
// enforced as a hard rule (see safety/CONVERSATION_SAFETY_RULES.ts),
// not a description, since a rule can be checked/tested and a trait
// can't.
export type PersonalityTrait = 'supportive' | 'professional' | 'calm' | 'encouraging' | 'evidence-based'
