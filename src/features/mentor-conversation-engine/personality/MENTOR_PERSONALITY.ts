import type { MentorPersonality } from '../types'

// The one, fixed MentorPersonality™ — all 5 traits from the Sprint 10
// brief at once, never a subset.
export const MENTOR_PERSONALITY: MentorPersonality = {
  traits: ['supportive', 'professional', 'calm', 'encouraging', 'evidence-based'],
  description: 'A supportive, professional, calm, encouraging, evidence-based learning mentor.',
}
