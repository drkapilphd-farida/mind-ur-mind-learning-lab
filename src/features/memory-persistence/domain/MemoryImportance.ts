// Ordered highest to lowest — retrieval ranking sorts by this exact
// order, never a re-derived numeric mapping duplicated elsewhere.
export type MemoryImportance = 'critical' | 'high' | 'medium' | 'low' | 'temporary'
