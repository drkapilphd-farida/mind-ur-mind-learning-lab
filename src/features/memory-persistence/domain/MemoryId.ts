// A plain string alias, not a branded/nominal type — consistent with
// every other id shape in this codebase's own conventions. Its own
// named type still documents intent at every call site.
export type MemoryId = string
