// Memory Indexing Infrastructure™ domain models (Sprint 16). Pure
// TypeScript, no framework dependency, intra-feature only (imports
// `MemoryId` from `../domain` — the same feature, not a cross-feature
// import).

export type { IndexKey } from './IndexKey'
export type { IndexType } from './IndexType'
export type { IndexEntry } from './IndexEntry'
export type { IndexMetadata } from './IndexMetadata'
export type { MemoryIndex } from './MemoryIndex'
