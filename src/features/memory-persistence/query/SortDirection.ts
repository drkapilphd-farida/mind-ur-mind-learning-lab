// 'descending' on `importance` means most-important-first (critical ->
// temporary) — see sorting/DefaultMemorySorter.ts for the exact
// numeric mapping.
export type SortDirection = 'ascending' | 'descending'
