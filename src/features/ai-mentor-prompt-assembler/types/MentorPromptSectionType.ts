// "System Context, Learner Context, Current Journey, Recommendations,
// Next Actions, Metadata" — the Sprint 30 brief's own Section 3 list,
// verbatim, and the fixed order every payload's sections are always
// composed in.
export type MentorPromptSectionType = 'system-context' | 'learner-context' | 'current-journey' | 'recommendations' | 'next-actions' | 'metadata'
