// A plain string alias, not a branded/nominal type — consistent with
// this codebase's own id conventions (see e.g.
// `@/features/memory-persistence/domain/MemoryId.ts`, independently
// mirrored here rather than imported — "No cross-feature imports").
export type SessionId = string
