// `MentorPromptPayload` always has exactly 6 sections (Sprint 30's own
// fixed design) — reused by both validation's coverage check and
// diagnostics' completeness calculation.
export const EXPECTED_SECTION_COUNT = 6
