// 30-Day Quantum Speed Reading Mastery + Live Cohort™ — the real,
// hosted Razorpay Payment Link for the ₹4,999 enrollment. A single
// source of truth so the dashboard hero and the pricing page never risk
// drifting to two different URLs. Completing this checkout takes real
// payment; it does not automatically grant any in-app access (no
// entitlement is wired to it) — enrollment/scheduling is handled
// manually after payment, so no surrounding copy should promise instant
// or automatic access.
export const RAZORPAY_MASTERCLASS_PAYMENT_LINK = 'https://rzp.io/rzp/h8zlaJ0'
